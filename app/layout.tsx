import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Header from "./_components/Header";
import Footer from "./_components/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Sravan Kachavarapu — Frontend Engineer",
  description:
    "Senior Software Engineer specializing in inclusive end-user experiences, design systems, and accessibility.",
  metadataBase: new URL("https://sravankacha.com"),
  openGraph: {
    title: "Sravan Kachavarapu",
    description: "Frontend Engineer · Design Systems · Accessibility",
    url: "https://sravankacha.com",
    siteName: "Sravan Kachavarapu",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-background text-foreground">
        <Header />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
